<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="challengeyourknowledges.appsyouneed.idinu.challengeyourknowledges">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".game.GameActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".game.GameOverActivity" />
        <activity android:name=".rules.RulesActivity" />
        <activity android:name=".game.CountdownActivity" />
        <activity android:name=".castigaIncercari.CastigaIncercariActivity"></activity>
        <activity android:name=".tipsTricks.TipsAndTricksActivity"></activity>
    </application>

</manifest>